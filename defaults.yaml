tags: 
  - TheStudio
  - Backend
environment:
  USERS_TABLE: ${self:provider.stage}-${self:service}-users
  CLASSES_TABLE: ${self:provider.stage}-${self:service}-classes-v2
  CODES_TABLE: ${self:provider.stage}-${self:service}-verification-codes
  PACKAGES_TABLE: ${self:provider.stage}-${self:service}-packages
  JWT_SECRET: pX6kDWovpzjkfon